<view class="page">
  <view class="tip">请输入13位条形码，即时输入即时生成</view>
  <input value="{{ number }}" type="number" class="input" bindinput="bindinput" />

  <v-barcode v-class="barcode" number="{{ number }}" options="{{ options }}" width="200" height="100" />
</view>