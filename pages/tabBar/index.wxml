<view class="page">

  <view class="list" wx:for="{{list}}" wx:key="title" wx:for-item="ite">
    <view class="list-title">{{ite.title}}</view>
    <navigator wx:for="{{ite.ls}}" wx:key="title" url="{{item.path}}" class="arrow-right">{{item.title}}</navigator>
  </view>

</view>